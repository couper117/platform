# Loaded by `vite build --mode demo` (npm run build:demo).
# Swaps the axios client for the local mock adapter so the built site in
# demo/dist runs with no backend, database, or network access.
VITE_DEMO=true
