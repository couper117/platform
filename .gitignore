# Dependencies
node_modules/
.pnp
.pnp.js

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
activity.log

# Build outputs
dist/
build/
.vercel

# ...except the self-contained showcase build, which is committed on purpose so
# a fresh clone can run `node demo/serve.js` with no toolchain.
# Regenerate with: cd apps/frontend && npm run build:demo
!demo/dist/
!demo/dist/**

# Editor/OS
.vscode/
.idea/
.DS_Store
*.swp
*.swo

# Database
/apps/backend/prisma/*.db
/apps/backend/prisma/*.db-journal

# Sharp/Image processing
/apps/backend/uploads/*
!/apps/backend/uploads/.gitkeep

# local QA/PDF scratch (playwright)
.qa-harness/

# Local DB reset helper (contains plaintext password) — never commit
**/reset-local-pg.ps1
Admin_Credentials.pdf

# Redesign before/after acceptance captures (360x800). Local evidence only.
design-refs/captures/

# Stray local scratch: ad-hoc screenshots and an unrelated project checked out here
Screenshot *.png
nga-tourism-project-main/

# Local DB backups (scripts/backup-db.mjs)
backups/

.env*
